# 3단계_머신러닝 이론

---

## 개요

3단계는 "이론을 실전 모델링으로 연결"하는 단계입니다.  
알고리즘 이름을 외우는 것이 아니라, 문제 유형/데이터 특성/평가 기준에 맞춰 모델을 선택하고 검증하는 능력을 만드는 것이 목표입니다.

---

## 학습 목표

1. 분류/회귀/군집 문제에서 적절한 알고리즘을 선택할 수 있다.  
2. 과적합/과소적합을 진단하고 완화 전략을 적용할 수 있다.  
3. 불균형 데이터에서 지표를 올바르게 해석할 수 있다.  
4. 교차검증과 하이퍼파라미터 탐색을 재현 가능하게 실행할 수 있다.  
5. 확률 보정과 임계값 최적화를 비용 관점에서 설명할 수 있다.  
6. 성능 지표의 불확실성(신뢰구간)을 추정할 수 있다.

---

## 꼭 알아야 할 핵심 설명 (서술형)

머신러닝의 핵심은 "학습 데이터 성능"이 아니라 "보지 못한 데이터 성능(일반화)"입니다.  
학습 점수가 높아도 배포 후 성능이 낮다면 모델링은 실패입니다.

따라서 모델 개발은 항상 3가지 질문을 함께 다뤄야 합니다.  
1) 문제 유형에 맞는 알고리즘인가?  
2) 데이터 누수/과적합이 없는가?  
3) 지표가 실제 목적(비용/위험)을 반영하는가?

또한 머신러닝은 한 번의 시도로 끝나지 않습니다.  
베이스라인 -> 오류 분석 -> 개선 실험 -> 재검증의 반복이 핵심이며, 실험 기록이 남아야 다음 개선이 가능합니다.

---

## 세부 이론

### 1) 대표 알고리즘과 사용 맥락

- **선형/로지스틱 회귀**: 해석 가능성 높고 베이스라인에 적합
- **의사결정나무/랜덤포레스트/GBDT**: 표형(tabular) 데이터에서 강력
- **SVM**: 경계가 복잡하지만 데이터 크기가 중간일 때 유용
- **KNN**: 단순하고 직관적이나 대규모 데이터에서 비효율
- **K-Means/PCA**: 비지도 탐색(군집/차원축소)

### 2) 편향-분산과 과적합

- **편향(Bias)** 높음 -> 모델이 너무 단순
- **분산(Variance)** 높음 -> 학습 데이터에 과하게 적합
- 과적합 완화:
  - 정규화
  - 모델 단순화
  - 특성 축소/선택
  - 데이터 증강/더 많은 데이터

### 3) 평가 지표 해석

- Accuracy: 전체 정답 비율
- Precision: 양성 예측 중 실제 양성 비율
- Recall: 실제 양성 중 잡아낸 비율
- F1: Precision/Recall 균형
- ROC-AUC: 임계값 변화 전반의 분리 성능

불균형 데이터에서는 Accuracy가 실제 품질을 가릴 수 있습니다.

### 4) 검증 전략

- Hold-out split
- Stratified K-Fold (분류에서 권장)
- Time series split (시계열에서 필수)
- Nested CV (엄밀한 모델 선택/평가 분리)

### 5) 특성 엔지니어링

- 결측치 처리(평균/중앙값/모델 기반)
- 범주형 인코딩(One-hot/Target Encoding 주의)
- 스케일링(Standard/MinMax)
- 로그 변환/클리핑으로 이상치 완화

### 6) 모델 해석 가능성(Interpretability)

모델 성능이 높아도 "왜 그런 예측을 했는가?"를 설명할 수 있어야 실무 적용이 쉽습니다.

- 전역 해석: 특성 중요도, 계수 해석
- 지역 해석: 특정 샘플의 예측 근거
- 도구 예시: Permutation Importance, SHAP, PDP

특히 규제 산업에서는 해석 가능성이 성능만큼 중요합니다.

### 7) 확률 보정(Calibration)

분류 모델의 출력 확률은 "순위"에는 유용하지만, 실제 확률과 다를 수 있습니다.  
예를 들어 0.9 확률이라도 실제 정답률이 0.7이라면 의사결정 위험이 커집니다.

- 보정 방법: Platt Scaling, Isotonic Regression
- 확인 방법: Calibration curve, Brier score

### 8) 데이터/개념 드리프트 기초

- Data Drift: 입력 분포 변화
- Concept Drift: 입력-정답 관계 변화

배포 후 성능 저하는 모델 자체 문제가 아니라 환경 변화일 수 있으므로,  
정기적인 드리프트 점검과 재학습 정책이 필요합니다.

### 9) 회귀 평가와 잔차 분석

회귀 문제에서는 분류 지표를 그대로 쓰면 해석이 틀어집니다.

- MAE: 평균 절대 오차(이해 쉬움, 이상치 덜 민감)
- RMSE: 큰 오차에 민감(품질 리스크 반영)
- R2: 기준 모델 대비 설명력

또한 잔차(residual) 패턴을 확인해야 합니다.
- 잔차 평균이 0에 가까운가?
- 특정 구간에서 잔차가 커지지 않는가?
- 예측값에 따라 분산이 커지는 이분산성이 없는가?

### 10) 불균형 데이터 대응 전략

불균형 데이터에서 핵심은 "모델 학습"과 "의사결정 임계값"을 분리해서 보는 것입니다.

학습 단계:
- class_weight 적용
- 언더샘플링/오버샘플링
- 앙상블 기반 불균형 대응 모델

의사결정 단계:
- PR-AUC 중심 비교
- 비용 기반 threshold 선택
- 업무 비용표(FP 비용, FN 비용) 기반 운영 룰

### 11) 모델 성능 비교의 통계적 신뢰성

모델 A가 모델 B보다 0.01 높다고 바로 우수하다고 결론 내리면 위험합니다.  
데이터 샘플링 변동 때문에 우연 차이일 수 있기 때문입니다.

권장 방법:
- 교차검증 분포 비교
- bootstrap 기반 신뢰구간 추정
- 동일 데이터 split에서 paired 비교

### 12) 시계열/그룹 단위 검증 주의

일반 K-Fold는 독립 샘플 가정이 강합니다.  
시계열이나 사용자 단위 데이터에서는 누수가 생기기 쉽습니다.

- 시계열: TimeSeriesSplit 사용
- 사용자/세션 단위: GroupKFold 사용
- 이벤트 예측: 시간 순서 기반 hold-out

### 13) 인과(Causality)와 예측(Prediction) 구분

머신러닝은 주로 예측 정확도에 최적화됩니다.  
하지만 "정책을 바꿨을 때 결과가 변하는가?"는 인과 질문입니다.

예측 성능이 높아도, 개입 효과를 보장하지 않습니다.  
의사결정 시스템에서는 예측 모델과 인과 분석 프레임을 분리해 해석해야 합니다.

---

## 실무에서 자주 틀리는 포인트

1. Test set을 튜닝에 반복 사용  
2. 목표 지표와 비즈니스 목적이 불일치  
3. 범주형 인코딩을 분할 전에 fit  
4. 클래스 불균형인데 Accuracy만 보고 의사결정  
5. 재현성(random_state, seed) 미고정  
6. 회귀 문제에서 RMSE/잔차 분석 없이 R2만으로 판단  
7. 시계열 데이터에 일반 K-Fold 적용  
8. 모델 간 미세 성능차를 통계적 검증 없이 확정

---

## Python 샘플 코드 (검증 + 튜닝 + 지표)

```python
from sklearn.datasets import load_breast_cancer
from sklearn.model_selection import train_test_split, StratifiedKFold, GridSearchCV
from sklearn.pipeline import Pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.metrics import classification_report, roc_auc_score
from sklearn.linear_model import LogisticRegression

# 데이터
X, y = load_breast_cancer(return_X_y=True)
X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.2, stratify=y, random_state=42
)

# 파이프라인 + 하이퍼파라미터 탐색
pipe = Pipeline(
    steps=[
        ("scaler", StandardScaler()),
        ("model", LogisticRegression(max_iter=3000, random_state=42)),
    ]
)

param_grid = {
    "model__C": [0.01, 0.1, 1.0, 10.0],
    "model__penalty": ["l2"],
}

cv = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)
search = GridSearchCV(
    estimator=pipe,
    param_grid=param_grid,
    scoring="f1",
    cv=cv,
    n_jobs=-1,
    refit=True,
)
search.fit(X_train, y_train)

best_model = search.best_estimator_
pred = best_model.predict(X_test)
proba = best_model.predict_proba(X_test)[:, 1]

print("best_params:", search.best_params_)
print(classification_report(y_test, pred, digits=4))
print("test_roc_auc:", round(roc_auc_score(y_test, proba), 4))
```

확률 보정(calibration) 및 PR-AUC 예시:

```python
from sklearn.calibration import CalibratedClassifierCV
from sklearn.metrics import average_precision_score, brier_score_loss

calibrated = CalibratedClassifierCV(best_model, method="isotonic", cv=3)
calibrated.fit(X_train, y_train)

proba_cal = calibrated.predict_proba(X_test)[:, 1]
pr_auc = average_precision_score(y_test, proba_cal)
brier = brier_score_loss(y_test, proba_cal)

print("calibrated_pr_auc:", round(pr_auc, 4))
print("calibrated_brier:", round(brier, 4))
```

비지도 샘플(탐색용):

```python
from sklearn.datasets import load_wine
from sklearn.preprocessing import StandardScaler
from sklearn.decomposition import PCA
from sklearn.cluster import KMeans

X, _ = load_wine(return_X_y=True)
X_scaled = StandardScaler().fit_transform(X)
X_2d = PCA(n_components=2, random_state=42).fit_transform(X_scaled)

kmeans = KMeans(n_clusters=3, random_state=42, n_init="auto")
labels = kmeans.fit_predict(X_2d)
print("cluster counts:", {i: int((labels == i).sum()) for i in range(3)})
```

회귀 평가 + 잔차 요약 예시:

```python
import numpy as np
from sklearn.datasets import load_diabetes
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_absolute_error, mean_squared_error, r2_score

Xr, yr = load_diabetes(return_X_y=True)
Xr_train, Xr_test, yr_train, yr_test = train_test_split(
    Xr, yr, test_size=0.2, random_state=42
)

reg = LinearRegression()
reg.fit(Xr_train, yr_train)
yr_pred = reg.predict(Xr_test)
residual = yr_test - yr_pred

mae = mean_absolute_error(yr_test, yr_pred)
rmse = np.sqrt(mean_squared_error(yr_test, yr_pred))
r2 = r2_score(yr_test, yr_pred)

print("mae:", round(mae, 4))
print("rmse:", round(rmse, 4))
print("r2:", round(r2, 4))
print("residual_mean:", round(float(np.mean(residual)), 4))
print("residual_std:", round(float(np.std(residual)), 4))
```

bootstrap으로 F1 신뢰구간 추정 예시:

```python
import numpy as np
from sklearn.metrics import f1_score

rng = np.random.default_rng(42)
n = len(y_test)
scores = []
for _ in range(500):
    idx = rng.integers(0, n, n)
    scores.append(f1_score(y_test[idx], pred[idx]))

low, high = np.percentile(scores, [2.5, 97.5])
print("f1_mean:", round(float(np.mean(scores)), 4))
print("f1_95ci:", (round(float(low), 4), round(float(high), 4)))
```

---

## 미니 과제

1. `scoring`을 `f1`에서 `roc_auc`로 바꿔 최적 파라미터 비교  
2. 랜덤포레스트로 동일 실험을 수행하고 성능/학습시간 비교  
3. 불균형 데이터에서 `class_weight="balanced"` 적용 전후 비교  
4. calibration 적용 전/후 Brier score 비교  
5. 회귀 예시에서 MAE/RMSE/R2가 서로 다른 결론을 주는 사례 작성  
6. bootstrap 신뢰구간 폭이 넓어지는 조건(샘플 수, 분산) 설명

---

## 핵심 용어

- Supervised Learning, Unsupervised Learning
- Classification, Regression, Clustering
- Feature Engineering, Pipeline, Cross Validation
- Accuracy, Precision, Recall, F1, ROC-AUC, PR-AUC
- Overfitting, Underfitting, Data Leakage, Bias-Variance
- Calibration, Brier Score, Threshold Optimization
- Residual Analysis, MAE, RMSE, R2
- Bootstrap, Confidence Interval, GroupKFold, TimeSeriesSplit

---

## 추천 자료

- [An Introduction to Statistical Learning (ISLR)](https://www.statlearning.com/)
- [scikit-learn User Guide](https://scikit-learn.org/stable/user_guide.html)
- 도서: `An Introduction to Statistical Learning`
- 도서: `The Elements of Statistical Learning`
- 도서: `Interpretable Machine Learning` (Molnar, 온라인 공개서)

---

## 이해도 점검 질문

1. 문제 유형별로 베이스라인 모델을 어떻게 선택할 것인가?  
2. Accuracy가 높은데 서비스 품질이 낮을 수 있는 이유는 무엇인가?  
3. Calibration이 필요한 상황을 2가지 이상 설명할 수 있는가?  
4. Data Drift와 Concept Drift를 구분해 설명할 수 있는가?  
5. 회귀에서 MAE와 RMSE가 다른 의사결정을 만드는 이유는 무엇인가?  
6. PR-AUC를 ROC-AUC보다 우선해야 하는 조건은 언제인가?  
7. bootstrap 신뢰구간을 사용해야 하는 이유를 설명할 수 있는가?  
8. 해석 가능성이 중요한 산업 도메인 예시를 들 수 있는가?

---

다음 학습: [4단계_딥러닝 이론.md](4단계_딥러닝%20이론.md)
