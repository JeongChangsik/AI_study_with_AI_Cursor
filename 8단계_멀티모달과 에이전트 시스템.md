# 8단계_멀티모달과 에이전트 시스템

---

## 개요

8단계는 단일 모델 예측을 넘어, **여러 입력 형태(텍스트/이미지/구조화 데이터)**와  
**도구 호출이 가능한 에이전트형 시스템**을 설계하는 단계입니다.

핵심은 모델 하나의 성능이 아니라,  
"계획 -> 검색 -> 도구 실행 -> 검증 -> 응답" 파이프라인 전체의 품질을 관리하는 것입니다.

---

## 학습 목표

1. 멀티모달 표현학습의 기본 원리를 설명할 수 있다.  
2. early/late/cross-attention 융합 전략의 차이를 설명할 수 있다.  
3. 멀티모달 RAG 파이프라인을 설계할 수 있다.  
4. 에이전트의 계획/실행/검증 루프를 구성할 수 있다.  
5. 도구 호출 안전성(권한/검증/타임아웃) 정책을 설계할 수 있다.  
6. 에이전트 성능을 task success + 비용 + 안전성으로 평가할 수 있다.

---

## 꼭 알아야 할 핵심 설명 (서술형)

멀티모달 AI의 본질은 "형식이 다른 정보를 같은 의미 공간으로 연결"하는 것입니다.  
텍스트, 이미지, 표형 데이터는 표현 방식이 다르지만, 최종적으로는 같은 문제를 푸는 근거로 통합되어야 합니다.

에이전트 시스템은 LLM이 직접 모든 정답을 생성하는 방식이 아니라,  
필요한 순간에 외부 도구를 호출해 계산/검색/실행을 분리합니다.  
따라서 품질의 핵심은 프롬프트보다 **도구 선택 정확도, 실행 안정성, 결과 검증 로직**에 있습니다.

멀티모달+에이전트 환경에서는 실패 형태도 다양해집니다.  
오검색, 잘못된 도구 선택, 도구 실행 실패, 정책 위반, 근거 없는 최종 응답이 발생할 수 있으므로  
중간 단계별 로그와 검증기가 필수입니다.

---

## 세부 이론

### 1) 멀티모달 표현학습

- 텍스트 인코더와 이미지 인코더를 같은 임베딩 공간으로 정렬
- contrastive learning으로 의미 정렬(가까운 쌍은 가깝게, 먼 쌍은 멀게)
- 단일 점수보다 검색 순위(top-k) 품질이 중요

### 2) 모달 융합 전략

- **Early Fusion**: 입력 단계에서 결합(단순, 노이즈 민감)
- **Late Fusion**: 모달별 예측 후 결합(안정적, 상호작용 제한)
- **Cross-Attention Fusion**: 모달 간 상호작용 학습(표현력 높음, 비용 큼)

### 3) 멀티모달 RAG

기본 흐름:
1. 문서/이미지 캡션/메타데이터 인덱싱
2. 질의 유형 판별(텍스트 중심 vs 이미지 중심)
3. 모달별 검색 + 하이브리드 재랭킹
4. 컨텍스트 구성(토큰 예산 내)
5. 근거 포함 응답 생성

### 4) 에이전트 아키텍처

- Planner: 문제를 단계로 분해
- Tool Router: 어떤 도구를 쓸지 선택
- Executor: 실제 도구 호출
- Critic/Verifier: 결과 검증 및 재시도 결정

좋은 에이전트는 "한 번에 정답"보다 "실패해도 회복 가능한 구조"를 가집니다.

### 5) 메모리 설계

- 단기 메모리: 현재 세션 문맥
- 장기 메모리: 사용자 선호, 과거 작업 기록
- 작업 메모리: 현재 플랜 상태와 중간 결과

메모리는 정확도 향상에도 도움이 되지만, 개인정보/권한 관리 이슈가 함께 생깁니다.

### 6) 도구 호출 안전성

필수 정책:
- 화이트리스트 기반 도구 허용
- 입력 검증(파라미터 타입/범위)
- 실행 제한(타임아웃, 재시도 횟수)
- 민감 동작(삭제/결제/전송) 이중 확인

### 7) 평가 체계

에이전트 평가는 단일 지표로 불가능합니다.

- Task Success Rate
- Tool Call Precision/Recall
- Groundedness (근거 일치율)
- Latency/Cost per task
- Policy Violation Rate

---

## 실무에서 자주 틀리는 포인트

1. 멀티모달 검색을 단일 텍스트 검색으로 대체  
2. 도구 호출 실패를 예외 처리 없이 무시  
3. 플랜 단계별 로그 없이 최종 응답만 저장  
4. 고위험 도구에 권한 분리 없이 직접 연결  
5. 비용/지연 측정 없이 기능만 늘려 운영 불가 상태 초래

---

## Python 샘플 코드 (에이전트 루프 + 하이브리드 검색)

간단한 에이전트 루프 예시(플랜 -> 도구 -> 검증):

```python
from dataclasses import dataclass
from typing import Callable, Any


@dataclass
class ToolResult:
    ok: bool
    output: Any
    error: str = ""


def calc_tool(expr: str) -> ToolResult:
    try:
        # 안전성 예시: 숫자/연산자만 허용
        allowed = set("0123456789+-*/(). ")
        if not set(expr) <= allowed:
            return ToolResult(False, None, "unsafe expression")
        return ToolResult(True, eval(expr, {"__builtins__": {}}, {}))
    except Exception as e:
        return ToolResult(False, None, str(e))


TOOLS: dict[str, Callable[[str], ToolResult]] = {"calc": calc_tool}


def planner(user_query: str) -> list[tuple[str, str]]:
    if "계산" in user_query:
        expr = user_query.replace("계산", "").strip()
        return [("calc", expr)]
    return [("none", user_query)]


def verifier(result: ToolResult) -> bool:
    return result.ok


query = "계산 12 * (3 + 4)"
plan = planner(query)
print("plan:", plan)

for tool_name, arg in plan:
    if tool_name == "none":
        print("final:", arg)
        continue
    if tool_name not in TOOLS:
        print("error: unknown tool")
        continue
    out = TOOLS[tool_name](arg)
    if verifier(out):
        print("final:", out.output)
    else:
        print("tool_failed:", out.error)
```

멀티모달 메타데이터 하이브리드 검색 예시:

```python
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.metrics.pairwise import cosine_similarity
import numpy as np

# 이미지/문서를 텍스트 메타데이터로 표현한 간단 예시
items = [
    {"id": "img_1", "text": "red car on road", "tags": "vehicle transport outdoor"},
    {"id": "img_2", "text": "cat on sofa", "tags": "animal indoor pet"},
    {"id": "doc_1", "text": "vehicle insurance policy", "tags": "finance contract risk"},
]

corpus = [f"{x['text']} {x['tags']}" for x in items]
vec = TfidfVectorizer()
X = vec.fit_transform(corpus)


def retrieve(query: str, top_k: int = 2, alpha: float = 0.8):
    q = vec.transform([query])
    dense = cosine_similarity(q, X).flatten()

    # 단순 sparse score: 태그 overlap
    q_tokens = set(query.lower().split())
    sparse = np.array([
        len(q_tokens & set((it["tags"] + " " + it["text"]).lower().split())) / max(len(q_tokens), 1)
        for it in items
    ])

    score = alpha * dense + (1 - alpha) * sparse
    idx = np.argsort(score)[::-1][:top_k]
    return [(items[i]["id"], float(score[i])) for i in idx]


print(retrieve("vehicle risk policy"))
```

---

## 미니 과제

1. planner를 규칙 기반에서 점수 기반으로 확장  
2. 도구 실패 시 재시도/대체 도구 fallback 구현  
3. 검색 결과에 출처 신뢰도 점수를 추가해 재정렬  
4. task success + latency + violation rate를 함께 기록하는 평가표 작성

---

## 핵심 용어

- Multimodal Embedding, Contrastive Learning
- Early/Late Fusion, Cross-Attention
- Agent Planner, Tool Router, Verifier
- Groundedness, Tool Call Accuracy
- Trust Boundary, Fallback, Retry Policy

---

## 추천 자료

- [OpenAI Cookbook - Tool use patterns](https://cookbook.openai.com/)
- [LangChain/LangGraph Concept Docs](https://python.langchain.com/)
- 도서: `Designing Machine Learning Systems`
- 도서: `Speech and Language Processing`

---

## 이해도 점검 질문

1. 멀티모달 융합에서 early/late/cross-attention의 트레이드오프는?  
2. 에이전트에서 planner와 verifier를 분리해야 하는 이유는?  
3. 도구 호출 안전성 정책을 최소 3개 이상 설명할 수 있는가?  
4. 멀티모달 RAG에서 검색 실패가 발생하는 대표 원인은 무엇인가?  
5. 운영 지표를 단일 정확도 대신 다축으로 봐야 하는 이유는?

---

다음 학습: [9단계_AI 연구와 논문 재현.md](9단계_AI%20연구와%20논문%20재현.md)
