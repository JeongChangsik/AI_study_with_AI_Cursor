# 2단계_수학과 통계 기초

---

## 개요

2단계의 목표는 AI 모델이 왜 그렇게 동작하는지 **수학적으로 설명할 수 있는 상태**가 되는 것입니다.  
공식 암기가 목적이 아니라, 선형대수/미분/확률통계를 실제 모델 학습과 연결하는 것이 핵심입니다.

---

## 학습 목표

1. 벡터/행렬 연산이 모델 입력과 파라미터를 어떻게 표현하는지 설명할 수 있다.  
2. 경사하강법의 원리와 학습률의 의미를 설명할 수 있다.  
3. 확률분포, 기댓값, 분산을 모델 평가 관점에서 해석할 수 있다.  
4. 베이즈 정리를 분류 문제의 직관과 연결할 수 있다.  
5. Jacobian/Hessian과 곡률 개념을 최적화와 연결해 설명할 수 있다.  
6. Likelihood, MLE, MAP의 차이를 설명할 수 있다.  
7. 수치안정성(underflow/overflow) 문제를 코드 수준에서 인지하고 대응할 수 있다.

---

## 꼭 알아야 할 핵심 설명 (서술형)

AI 모델은 결국 함수 근사기입니다.  
입력을 벡터로 받고, 파라미터(가중치)와 연산해 출력을 만듭니다. 이 과정이 행렬 연산으로 구현되기 때문에 선형대수는 AI의 기본 문법입니다.

학습은 손실함수를 최소화하는 과정입니다.  
미분을 통해 손실이 증가/감소하는 방향을 알고, 경사하강법으로 파라미터를 조금씩 업데이트합니다.  
학습률이 너무 크면 발산하고, 너무 작으면 학습이 매우 느려집니다.

데이터는 항상 불확실성을 포함합니다.  
확률과 통계는 이 불확실성을 수치로 다루는 도구이며, 평균/분산/분포를 이해해야 모델 성능 해석이 가능합니다.  
특히 분산이 큰 데이터에서는 모델이 불안정해질 수 있어 검증 전략이 중요해집니다.

---

## 세부 이론

### 1) 선형대수 핵심

- **벡터(Vector)**: 특징(feature)의 집합
- **행렬(Matrix)**: 여러 샘플의 벡터 집합
- **내적(Dot Product)**: 유사도/가중합 계산의 핵심
- **행렬곱(Matrix Multiplication)**: 신경망 층 연산의 기본

선형회귀 예시:
- 입력 \(x\), 가중치 \(w\), 편향 \(b\)에 대해 `y_hat = w*x + b`
- 다변량에서는 `y_hat = XW + b`

### 2) 미분과 최적화

- **손실함수(Loss)**: 예측 오차를 수치화
- **기울기(Gradient)**: 손실 증가 방향
- **경사하강법**: `w = w - lr * grad`
- **연쇄법칙(Chain Rule)**: 딥러닝 역전파 계산의 기반

### 3) 확률/통계 핵심

- **기댓값(E[X])**: 평균적 경향
- **분산(Var[X])**: 흩어짐 정도
- **확률분포**: 데이터 생성 패턴
- **조건부확률 P(A|B)**: B가 주어졌을 때 A 확률
- **베이즈 정리**: 사전확률 + 관측으로 사후확률 갱신

### 4) 모델 해석과 통계 연결

- 샘플 수가 적고 분산이 크면 모델 변동성 증가
- 평균 성능만 보지 말고 분산/표준편차도 함께 확인
- 교차검증으로 성능 분포를 확인하면 과대해석을 줄일 수 있음

### 5) 선형대수 심화: 고유값/고유벡터와 PCA

PCA는 공분산 행렬의 고유벡터를 이용해 분산이 큰 축으로 데이터를 투영합니다.  
이때 고유값은 각 축이 설명하는 분산 크기를 의미합니다.  
즉, 차원축소는 단순 압축이 아니라 "정보 손실을 최소화한 표현 변경"입니다.

### 6) 통계적 추론 기초

- 표본평균은 표본 수가 커질수록 안정됩니다(큰수의 법칙).
- 신뢰구간은 "추정값의 불확실성 범위"를 나타냅니다.
- 가설검정(p-value)은 효과 크기 자체가 아니라 관측 결과의 우연 가능성을 보여줍니다.

AI 실무에서는 p-value 하나만 보는 것이 아니라, 효과 크기와 재현성을 함께 확인해야 합니다.

### 7) 벡터화(Vectorization)의 중요성

Python for-loop보다 NumPy 벡터화 연산이 빠른 이유는 내부적으로 최적화된 저수준 연산을 사용하기 때문입니다.  
데이터 전처리/손실 계산/gradient 계산을 벡터화하면 실험 속도와 코드 안정성이 함께 좋아집니다.

### 8) 벡터공간, 기저, 선형독립

선형대수에서 중요한 질문은 "이 데이터가 몇 차원 정보로 설명되는가?"입니다.

- **선형독립**: 어떤 벡터도 다른 벡터 조합으로 표현되지 않음
- **기저(Basis)**: 공간을 생성하는 최소 벡터 집합
- **랭크(Rank)**: 행렬이 담고 있는 독립 정보의 수

이 개념은 특성 중복, 차원축소, 모델 불안정성(다중공선성) 이해에 직접 연결됩니다.

### 9) 노름, 거리, 유사도

- L1 norm: 희소성 유도에 유리(Lasso와 연결)
- L2 norm: 큰 값에 더 민감(Weight decay와 연결)
- Cosine similarity: 크기보다 방향 유사도 비교

노름 선택은 모델의 일반화 성능과 최적화 경로에 영향을 줍니다.

### 10) Jacobian/Hessian과 곡률

- **Jacobian**: 벡터 함수의 1차 미분 정보
- **Hessian**: 스칼라 함수의 2차 미분 정보(곡률)

직관:
- Gradient는 "어느 방향으로 내려갈지"
- Hessian은 "그 지형이 얼마나 가파른지/평평한지"

2차 정보는 뉴턴 방법 등 고급 최적화에서 중요하며, 손실 지형(plateau, saddle point) 이해에 도움을 줍니다.

### 11) Likelihood, MLE, MAP

- **Likelihood**: 파라미터가 주어졌을 때 데이터가 관측될 가능성
- **MLE**: likelihood를 최대화하는 파라미터 추정
- **MAP**: 사전분포(prior)를 반영한 추정

실무 관점에서는 MAP이 정규화(regularization)와 연결됩니다.  
예를 들어 L2 정규화는 가우시안 prior를 둔 MAP 추정으로 해석할 수 있습니다.

### 12) 표본추출, 중심극한정리, 불확실성

중심극한정리(CLT)는 표본 평균이 충분히 크면 정규분포에 가까워짐을 설명합니다.  
이 덕분에 신뢰구간 추정, 통계적 테스트, 성능 추정 오차 계산이 가능해집니다.

모델 비교에서는 "평균 성능 차이"뿐 아니라 "불확실성 범위"까지 함께 해석해야 합니다.

### 13) 수치안정성과 실전 계산

딥러닝/통계 계산에서는 underflow/overflow가 자주 발생합니다.

대표 사례:
- 매우 작은 확률 곱 계산 시 0으로 소실
- 지수(exp) 연산에서 무한대로 발산

대응 방법:
1. 로그 공간에서 계산(log-probability)
2. log-sum-exp trick 사용
3. 표준화/정규화로 입력 스케일 조정
4. dtype과 수치 범위 점검

---

## 실무에서 자주 틀리는 포인트

1. 행렬 차원(shape) 검증 없이 구현  
2. 미분 공식은 알지만 학습률의 의미를 모름  
3. 평균 성능만 보고 분산(안정성) 확인을 생략  
4. 확률값과 점수(logit)를 혼동  
5. 교차검증 없이 단일 split 결과만 신뢰  
6. 상관관계를 인과관계로 잘못 해석  
7. p-value만 보고 효과 크기/실무 영향 무시  
8. 수치안정성 문제를 버그로 오해하고 수식 수준 점검을 생략

---

## Python 샘플 코드 (선형대수 + 경사하강법)

```python
import numpy as np

# 1) 선형대수 기초: 내적/행렬곱
x = np.array([1.0, 2.0, 3.0])        # feature vector
w = np.array([0.2, -0.1, 0.5])       # weight vector
b = 0.3
y_hat = x @ w + b
print("예측값(y_hat):", y_hat)

# 2) 경사하강법으로 1차 선형회귀 학습: y = 2x + 1 + noise
rng = np.random.default_rng(42)
X = np.linspace(0, 10, 200)
y = 2.0 * X + 1.0 + rng.normal(0, 0.8, size=len(X))

w = 0.0
b = 0.0
lr = 0.01
epochs = 2000

for epoch in range(epochs):
    y_pred = w * X + b
    error = y_pred - y
    # MSE 기준 gradient
    dw = (2 / len(X)) * np.sum(error * X)
    db = (2 / len(X)) * np.sum(error)

    w -= lr * dw
    b -= lr * db

    if epoch % 400 == 0:
        loss = np.mean(error ** 2)
        print(f"epoch={epoch:4d}, loss={loss:.4f}, w={w:.4f}, b={b:.4f}")

print("학습 완료 파라미터:", {"w": round(w, 4), "b": round(b, 4)})
```

베이즈 정리 간단 계산 예시:

```python
# P(A|B) = P(B|A) * P(A) / P(B)
p_a = 0.01       # 질병 유병률
p_b_given_a = 0.95  # 질병 있을 때 양성 확률
p_b = 0.05       # 전체 양성 비율
p_a_given_b = p_b_given_a * p_a / p_b
print("양성일 때 실제 질병 확률:", round(p_a_given_b, 4))
```

유한차분으로 gradient 검증(미분 구현 점검):

```python
import numpy as np

def f(w):
    # 간단한 2차 함수
    return (w - 3.0) ** 2 + 1.0

def analytic_grad(w):
    return 2.0 * (w - 3.0)

def numerical_grad(w, eps=1e-6):
    return (f(w + eps) - f(w - eps)) / (2 * eps)

w0 = 1.234
g_a = analytic_grad(w0)
g_n = numerical_grad(w0)
print("analytic_grad:", g_a)
print("numerical_grad:", g_n)
print("abs_diff:", abs(g_a - g_n))
```

PCA 분산 설명력 확인 예시:

```python
from sklearn.datasets import load_wine
from sklearn.preprocessing import StandardScaler
from sklearn.decomposition import PCA

X, _ = load_wine(return_X_y=True)
X_scaled = StandardScaler().fit_transform(X)

pca = PCA(n_components=5, random_state=42)
pca.fit(X_scaled)

print("explained_variance_ratio:", pca.explained_variance_ratio_)
print("cumulative:", pca.explained_variance_ratio_.cumsum())
```

log-sum-exp 수치안정성 예시:

```python
import numpy as np

x = np.array([1000.0, 1001.0, 1002.0])

# 불안정한 계산 (overflow 가능)
unstable = np.log(np.sum(np.exp(x)))

# 안정한 계산
m = np.max(x)
stable = m + np.log(np.sum(np.exp(x - m)))

print("unstable:", unstable)
print("stable:", stable)
```

---

## 미니 과제

1. 경사하강법 코드에서 `lr`을 0.1, 0.001로 바꿔 수렴 속도 비교  
2. 데이터 노이즈 표준편차를 0.8 -> 2.0으로 바꿔 loss 변화를 확인  
3. 벡터 차원 불일치 오류를 의도적으로 만든 뒤 해결 방법 기록  
4. analytic vs numerical gradient 차이가 커지는 사례를 만들어 원인 분석  
5. PCA 누적 분산 90%를 만족하는 최소 차원 수 찾기  
6. log-sum-exp 적용 전/후 계산 안정성 비교

---

## 핵심 용어

- Vector, Matrix, Dot Product, Matrix Multiplication
- Gradient, Learning Rate, Chain Rule, Convex
- Jacobian, Hessian, Curvature, Saddle Point
- Distribution, Expectation, Variance, Standard Deviation
- Conditional Probability, Bayes Rule
- Likelihood, MLE, MAP, Prior, Posterior
- Numerical Stability, Log-Sum-Exp

---

## 추천 자료

- [Mathematics for Machine Learning](https://mml-book.github.io/)
- [Khan Academy - Statistics and Probability](https://www.khanacademy.org/math/statistics-probability)
- [3Blue1Brown - Linear Algebra](https://www.3blue1brown.com/topics/linear-algebra)
- 도서: `All of Statistics` (Larry Wasserman)
- 도서: `Pattern Recognition and Machine Learning` (Christopher Bishop)
- 도서: `Deep Learning` (Goodfellow et al., 수학 기초 장)

---

## 이해도 점검 질문

1. 행렬곱과 선형변환의 관계를 설명할 수 있는가?  
2. 학습률이 너무 크거나 작을 때 생기는 현상을 예시로 말할 수 있는가?  
3. PCA에서 고유값/고유벡터가 어떤 의미인지 설명할 수 있는가?  
4. Jacobian/Hessian의 차이를 최적화 관점에서 설명할 수 있는가?  
5. MLE와 MAP의 차이, 그리고 정규화와의 연결을 설명할 수 있는가?  
6. 평균 성능과 성능 분산을 함께 봐야 하는 이유를 설명할 수 있는가?  
7. 벡터화 연산이 실무 코드 품질에 주는 이점을 2가지 이상 설명할 수 있는가?  
8. log-sum-exp를 왜 써야 하는지 수치오차 관점에서 설명할 수 있는가?

---

다음 학습: [3단계_머신러닝 이론.md](3단계_머신러닝%20이론.md)
