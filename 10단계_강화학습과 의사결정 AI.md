# 10단계_강화학습과 의사결정 AI

---

## 개요

10단계는 정답이 미리 주어진 데이터셋을 학습하는 방식에서 한 단계 더 나아가,  
**행동(action) 자체가 미래 데이터를 바꾸는 환경**에서 학습하는 방법을 다룹니다.

검색 랭킹, 추천, 로봇 제어, 게임, 동적 가격, 운영 최적화처럼  
"지금의 선택이 다음 상태와 보상에 영향을 주는 문제"는 강화학습 관점이 필요합니다.

핵심은 모델 성능 숫자 하나가 아니라,  
상태-행동-보상의 장기 상호작용을 안정적으로 설계하고 검증하는 것입니다.

---

## 학습 목표

1. MDP(State/Action/Reward/Transition)의 구성 요소를 설명할 수 있다.  
2. Bellman 방정식과 가치함수(Value Function)의 의미를 설명할 수 있다.  
3. Q-learning, DQN, Policy Gradient, Actor-Critic의 차이를 설명할 수 있다.  
4. 탐험-활용(exploration-exploitation) 전략을 문제 상황에 맞게 선택할 수 있다.  
5. 보상 설계(reward design)와 reward hacking 위험을 식별할 수 있다.  
6. 로그 기반 오프라인 평가(IPS/SNIPS)의 기본 원리를 설명할 수 있다.  
7. 안전 제약과 인간 승인(HITL)을 포함한 RL 운영 정책을 설계할 수 있다.  
8. RL 실험을 재현 가능한 형태(시드/환경/체크포인트)로 관리할 수 있다.

---

## 꼭 알아야 할 핵심 설명 (서술형)

지도학습은 "정답 라벨"이 있고, 모델은 입력에서 정답을 맞히도록 학습합니다.  
반면 강화학습은 에이전트가 행동한 결과로 보상을 얻고, 그 경험을 통해 정책(policy)을 업데이트합니다.

중요한 차이는 **데이터 분포가 정책에 의해 바뀐다는 점**입니다.  
정책이 바뀌면 방문하는 상태가 달라지고, 따라서 수집되는 데이터도 달라집니다.  
이 특성 때문에 RL은 안정성, 탐험 전략, 오프라인 평가가 매우 중요합니다.

또한 RL 시스템의 실패는 정확도 하락보다 위험할 수 있습니다.  
보상이 잘못 설계되면 모델이 "보상을 속이는" 행동(reward hacking)을 선택할 수 있고,  
운영에서는 작은 정책 변경이 큰 사용자 경험 저하로 연결될 수 있습니다.

좋은 RL 실무는 "고성능 알고리즘 선택"보다  
문제 정의, 보상 설계, 안전 제약, 실험 재현성, 점진적 배포 전략을 먼저 정교하게 설계합니다.

---

## 세부 이론

### 1) MDP(마르코프 의사결정과정)

강화학습 문제는 일반적으로 MDP로 표현합니다.

- State(s): 현재 상황
- Action(a): 가능한 행동
- Reward(r): 행동의 즉시 보상
- Transition P(s'|s,a): 다음 상태 확률
- Discount(gamma): 미래 보상의 현재 가치 반영

마르코프 성질은 "다음 상태가 과거 전체가 아닌 현재 상태와 행동에만 의존"한다는 가정입니다.

### 2) Bellman 방정식과 가치함수

가치함수는 장기 보상의 기대값을 의미합니다.

- V(s): 상태 가치
- Q(s,a): 상태-행동 가치

Bellman 방정식은 "현재 가치 = 즉시 보상 + 할인된 미래 가치"의 재귀 구조를 제공합니다.  
이 구조가 Q-learning, DQN, Actor-Critic 등 대부분 알고리즘의 핵심 기반입니다.

### 3) DP / MC / TD 학습 패러다임

- Dynamic Programming(DP): 환경 전이 확률을 알고 있을 때 사용
- Monte Carlo(MC): 에피소드 종료 후 실제 리턴 평균으로 추정
- Temporal Difference(TD): 현재 추정값으로 부트스트래핑(온라인 업데이트)

실무에서는 환경 모델을 모르므로 TD 계열(Q-learning, SARSA, DQN)이 자주 사용됩니다.

### 4) Q-learning과 SARSA

- Q-learning: 오프폴리시, max_a Q(s',a) 사용
- SARSA: 온폴리시, 실제 선택된 a' 사용

Q-learning은 공격적으로 최적 정책으로 수렴하려는 경향이 있고,  
SARSA는 현재 행동 정책을 반영해 상대적으로 보수적일 수 있습니다.

### 5) 함수 근사와 DQN

상태 공간이 큰 경우 테이블 기반 Q를 쓸 수 없어 신경망으로 Q를 근사합니다.

DQN의 안정화 핵심:
1. Experience Replay: 샘플 상관성 완화
2. Target Network: 목표값 변동 완화
3. Gradient Clipping: 학습 폭주 방지

### 6) Policy Gradient와 Actor-Critic

행동을 직접 확률적으로 모델링하는 방식입니다.

- Policy Gradient: 정책 파라미터를 직접 업데이트
- Actor-Critic: Actor(정책) + Critic(가치 추정)
- PPO: 신뢰 영역을 근사해 급격한 정책 변화 억제

연속 제어나 고차원 행동 공간에서는 정책기반/Actor-Critic 계열이 강력합니다.

### 7) 탐험-활용(Exploration vs Exploitation)

강화학습은 좋은 정책을 찾기 위해 "모르는 행동"을 시도해야 합니다.

- epsilon-greedy: 확률적으로 무작위 행동
- softmax/boltzmann: 가치 기반 확률 선택
- UCB/Thompson Sampling: 불확실성 기반 탐험

탐험을 너무 줄이면 지역 최적점에 갇히고, 너무 늘리면 학습 효율이 급락합니다.

### 8) 컨텍스트 밴딧과 추천/랭킹

장기 상태 전이가 약하거나 1-step 의사결정이 핵심인 문제에서는  
MDP 전체보다 컨텍스트 밴딧이 더 실용적일 수 있습니다.

예시:
- 뉴스 추천 슬롯 선택
- 알림 발송 시점 선택
- 프로모션 오퍼 선택

### 9) 보상 설계와 Reward Hacking

모델은 "의도"가 아니라 "정의한 보상 함수"를 최적화합니다.

주의점:
1. 측정 가능한 지표가 실제 목표를 대변하는지 검증
2. 단기 지표와 장기 지표의 충돌 분석
3. 제약 조건(안전/공정성/비용)을 별도 항으로 모델링

### 10) 오프라인 RL과 로그 기반 평가

실서비스에서 무작정 온라인 탐험은 위험합니다.  
그래서 과거 로그로 정책을 사전 평가하는 오프라인 절차가 중요합니다.

- IPS(Inverse Propensity Scoring)
- SNIPS(Self-Normalized IPS)
- Doubly Robust(DR)

로그 정책의 편향이 크면 평가 분산이 커지므로 신뢰구간 해석이 필수입니다.

### 11) 안전한 RL 운영

안전성 강화를 위한 운영 정책:

- 고위험 액션 하드 제약(금지/상한)
- 저신뢰 상황 HITL 에스컬레이션
- 정책 변경 폭 제한(rollout guardrail)
- canary/shadow 배포로 단계적 검증

### 12) RL 실험 재현성

재현성 확보 체크 포인트:

- 환경 버전 고정(gym 등)
- random seed 다중 반복
- 평가 에피소드 수 고정
- 체크포인트/로그/설정 파일 동시 저장

RL은 분산이 큰 편이므로 단일 런 결과만으로 결론 내리면 위험합니다.

---

## 실무에서 자주 틀리는 포인트

1. 보상 정의가 실제 비즈니스 목표를 정확히 반영한다고 가정  
2. 온라인 A/B 없이 오프라인 점수만으로 정책 교체  
3. 단일 시드 성능으로 알고리즘 우열 판단  
4. 탐험 비용(사용자 경험 저하)을 모델링하지 않음  
5. 정책 업데이트 폭 제한 없이 대규모 즉시 배포  
6. 로그 정책 확률(propensity) 기록 없이 OPE 수행 시도  
7. 제약 조건(안전/비용/공정성)을 사후 룰로만 처리  
8. 실패 시 롤백 기준 없이 실험 진행

---

## Python 샘플 코드 (Q-learning + Bandit OPE)

간단한 GridWorld에서 Q-learning:

```python
from dataclasses import dataclass
import numpy as np


@dataclass
class GridWorld:
    size: int = 5
    goal: tuple[int, int] = (4, 4)
    max_step: int = 50

    def reset(self, seed: int | None = None):
        self.rng = np.random.default_rng(seed)
        self.pos = (0, 0)
        self.t = 0
        return self._state()

    def _state(self) -> int:
        return self.pos[0] * self.size + self.pos[1]

    def step(self, action: int):
        # 0:up, 1:down, 2:left, 3:right
        r, c = self.pos
        if action == 0:
            r = max(0, r - 1)
        elif action == 1:
            r = min(self.size - 1, r + 1)
        elif action == 2:
            c = max(0, c - 1)
        elif action == 3:
            c = min(self.size - 1, c + 1)

        self.pos = (r, c)
        self.t += 1
        done = self.pos == self.goal or self.t >= self.max_step
        reward = 10.0 if self.pos == self.goal else -0.1
        return self._state(), reward, done


def q_learning(episodes: int = 1500, alpha: float = 0.1, gamma: float = 0.99, eps: float = 0.2):
    env = GridWorld()
    n_state = env.size * env.size
    n_action = 4
    q = np.zeros((n_state, n_action), dtype=float)
    returns = []

    for ep in range(episodes):
        s = env.reset(seed=ep)
        total = 0.0
        epsilon = max(0.02, eps * (1 - ep / episodes))  # 간단 decay
        done = False
        while not done:
            if np.random.rand() < epsilon:
                a = np.random.randint(n_action)
            else:
                a = int(np.argmax(q[s]))
            ns, r, done = env.step(a)
            td_target = r + gamma * np.max(q[ns]) * (1 - int(done))
            q[s, a] += alpha * (td_target - q[s, a])
            s = ns
            total += r
        returns.append(total)
    return q, returns


q_table, ep_returns = q_learning()
print("last_100_episode_return_mean:", round(float(np.mean(ep_returns[-100:])), 4))
print("start_state_best_action:", int(np.argmax(q_table[0])))
```

로그 데이터 기반 IPS/SNIPS 오프라인 평가 예시(컨텍스트 밴딧):

```python
import numpy as np

# 예시 로그: (보상 r, 행동 a, 로깅정책확률 p_log, 타깃정책확률 p_tgt)
r = np.array([1, 0, 1, 0, 1, 1, 0, 1], dtype=float)
a = np.array([0, 1, 0, 1, 0, 1, 1, 0], dtype=int)
p_log = np.array([0.6, 0.7, 0.6, 0.7, 0.6, 0.7, 0.7, 0.6], dtype=float)
p_tgt = np.array([0.8, 0.2, 0.8, 0.2, 0.8, 0.2, 0.2, 0.8], dtype=float)

# 간단화: 선택된 행동의 중요도 가중치
w = p_tgt / p_log
ips = float(np.mean(w * r))
snips = float(np.sum(w * r) / np.sum(w))

print("IPS estimate:", round(ips, 4))
print("SNIPS estimate:", round(snips, 4))
```

---

## 미니 과제

1. GridWorld 보상 함수를 수정해 reward hacking 사례를 직접 재현  
2. epsilon 스케줄(linear/exponential)을 바꿔 학습 안정성 비교  
3. 시드 10개 반복으로 평균/표준편차/95% CI 보고서 작성  
4. Bandit 로그에 대해 IPS vs SNIPS 편향/분산 차이 분석  
5. 고위험 액션 차단 규칙(guardrail) 포함 정책 실행기 구현  
6. RL 정책 배포 롤백 조건 문서 작성(성능/안전/비용)

---

## 핵심 용어

- MDP, State, Action, Reward, Transition
- Bellman Equation, Value Function, Q Function
- On-policy, Off-policy, TD Learning
- DQN, Replay Buffer, Target Network
- Policy Gradient, Actor-Critic, PPO
- Exploration-Exploitation, Contextual Bandit
- OPE(IPS/SNIPS/DR), Propensity Score
- Reward Hacking, Guardrail, Canary Rollout

---

## 추천 자료

- 도서: `Reinforcement Learning: An Introduction` (Sutton & Barto)
- 강의: David Silver RL Course (UCL)
- 문서: [OpenAI Spinning Up in Deep RL](https://spinningup.openai.com/)
- 코드: [CleanRL](https://github.com/vwxyzjn/cleanrl)
- 도서: `Algorithms for Reinforcement Learning`

---

## 이해도 점검 질문

1. 지도학습과 강화학습에서 "데이터 생성 과정"의 차이는 무엇인가?  
2. Bellman 방정식이 RL 알고리즘 전반의 공통 기반이 되는 이유는 무엇인가?  
3. Q-learning과 SARSA의 정책 관점 차이는 무엇인가?  
4. DQN에서 replay buffer와 target network가 필요한 이유는 무엇인가?  
5. 보상 설계가 잘못되었을 때 어떤 형태의 reward hacking이 생길 수 있는가?  
6. 오프라인 로그 평가에서 IPS와 SNIPS를 함께 보는 이유는 무엇인가?  
7. RL 정책 배포 시 canary/shadow 전략이 필요한 이유는 무엇인가?  
8. RL 실험 재현성을 위해 반드시 고정/기록해야 할 항목은 무엇인가?

---

다음 학습: [11단계_데이터 엔지니어링과 AI 시스템 최적화.md](11단계_데이터%20엔지니어링과%20AI%20시스템%20최적화.md)

---

최상위 허브: [0단계_학습자료.md](0단계_학습자료.md)
