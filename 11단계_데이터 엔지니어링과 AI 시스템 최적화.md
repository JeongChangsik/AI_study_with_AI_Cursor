# 11단계_데이터 엔지니어링과 AI 시스템 최적화

---

## 개요

11단계는 모델 성능 자체보다,  
**데이터가 안정적으로 흐르고 서비스가 지속적으로 동작하는 시스템 역량**을 만드는 단계입니다.

실무에서 AI 실패의 상당수는 알고리즘이 아니라  
데이터 품질 문제, 스키마 변경, 피처 불일치, 지연시간 초과, 비용 폭증에서 시작됩니다.  
따라서 이 단계는 "모델 이후의 현실 문제"를 다룹니다.

---

## 학습 목표

1. AI 데이터 파이프라인(batch/stream)의 구조와 트레이드오프를 설명할 수 있다.  
2. 스키마 진화(schema evolution)와 데이터 계약 위반 대응 절차를 설계할 수 있다.  
3. 데이터 품질 지표(완전성/유일성/일관성/신선도)를 점검 자동화할 수 있다.  
4. 피처 스토어의 offline-online 일관성 요구사항을 설명할 수 있다.  
5. point-in-time join의 필요성과 누수 방지 원리를 설명할 수 있다.  
6. 추론 서빙에서 latency/throughput/cost의 균형점을 설계할 수 있다.  
7. 캐시/동적 배치/오토스케일링 전략을 서비스 특성에 맞게 선택할 수 있다.  
8. 운영 지표 기반으로 성능 저하 원인을 역추적하는 관측성 체계를 구성할 수 있다.

---

## 꼭 알아야 할 핵심 설명 (서술형)

모델이 아무리 좋아도, 입력 데이터가 깨지면 결과는 즉시 무의미해집니다.  
그래서 AI 시스템에서는 "모델 학습"보다 먼저 "데이터 신뢰성"을 설계해야 합니다.

특히 피처 엔지니어링에서는 훈련 시점과 예측 시점의 시간 경계를 엄격히 지켜야 합니다.  
미래 정보를 실수로 포함하면 오프라인 성능은 좋아 보이지만, 실제 배포에서는 급격히 무너집니다.

서빙 단계에서는 또 다른 현실 문제가 나타납니다.  
정확도를 조금 더 올리기 위해 응답 시간이 과도하게 느려지거나 비용이 폭증하면,  
비즈니스 기준에서 그 모델은 "좋은 모델"이 아닙니다.

즉 AI 제품 품질은  
"정확도 + 데이터 신뢰성 + 지연시간 + 비용 + 복구 가능성"의 균형으로 정의해야 합니다.

---

## 세부 이론

### 1) 데이터 파이프라인 기본 구조

일반적인 구성:

1. 수집(Ingestion): 로그, DB CDC, 외부 API
2. 정제(Validation/Cleansing): 타입/결측/중복 처리
3. 변환(Feature Build): 집계/정규화/인코딩
4. 저장(Storage): 데이터 레이크/웨어하우스/피처 스토어
5. 소비(Serving/Training): 온라인 추론, 배치 리포트, 모델 학습

실무에서는 "파이프라인 실패 시 어떻게 복구할지"가 설계 품질을 좌우합니다.

### 2) Batch vs Stream 처리

- Batch: 대량 처리, 비용 효율, 지연이 길어도 되는 업무에 적합
- Stream: 저지연 업데이트, 복잡도/운영비 증가

대부분의 팀은 하이브리드 구조를 사용합니다.  
핵심 지표는 스트림으로 빠르게 갱신하고, 무거운 집계는 배치로 안정적으로 처리합니다.

### 3) 스키마 진화와 데이터 계약

스키마 변경은 가장 흔한 장애 원인입니다.

- 컬럼 추가/삭제
- 타입 변경(int -> string)
- 범주값 변화(new enum)

대응 원칙:
1. backward compatible 우선
2. breaking change는 버전 분리
3. 계약 위반 시 fail-fast + 알림

### 4) 데이터 품질 지표

최소 지표:

- Completeness(완전성): 결측 비율
- Uniqueness(유일성): 키 중복 여부
- Validity(유효성): 값 범위/형식
- Consistency(일관성): 상호 규칙 위반 여부
- Freshness(신선도): 데이터 지연시간

품질 점검은 배치 완료 후 사람이 보는 리포트가 아니라,  
파이프라인 단계에서 자동으로 차단되어야 효과적입니다.

### 5) Feature Store와 일관성

피처 스토어의 핵심 목표는 재사용보다 **일관성**입니다.

- Offline Store: 학습 데이터 생성
- Online Store: 저지연 추론 피처 제공

동일한 feature definition을 두 경로에서 공유하지 않으면  
training-serving skew가 누적되고 디버깅 비용이 급증합니다.

### 6) Point-in-time Join과 데이터 누수 방지

학습 데이터 생성 시 타임스탬프 경계를 넘으면 미래정보 누수가 발생합니다.

원칙:
1. label_time 이전 데이터만 조인
2. late arriving data 정책 명시
3. 백필(backfill) 시 재현 가능한 스냅샷 고정

### 7) 추론 서빙 아키텍처

대표 패턴:

- Online API Serving: 실시간 응답(p95 지연 중요)
- Batch Inference: 대량 일괄 점수화(비용 효율 중요)
- Async Queue Serving: 지연 허용 업무에서 안정성 확보

서비스 목표에 따라 설계를 분리하지 않으면  
모든 요구를 한 시스템에 억지로 넣게 되어 운영이 불안정해집니다.

### 8) 성능 최적화: 지연/처리량/비용

주요 기법:

- 캐시(요청/피처/응답)
- 동적 배치(dynamic batching)
- 모델 압축(quantization/pruning/distillation)
- 비동기 I/O 및 병렬화

중요한 점은 "평균 지연"보다 p95/p99 tail latency를 관리하는 것입니다.

### 9) 오토스케일과 용량 계획

트래픽은 항상 변동합니다.  
고정 인프라는 과잉 비용 또는 장애를 동시에 유발할 수 있습니다.

- CPU/GPU utilization 기반 scale-out
- 요청 대기열 길이(queue depth) 기반 확장
- 최소/최대 레플리카 제한
- 급증 트래픽 대비 pre-warm 전략

### 10) 관측성(Observability)과 디버깅

필수 구성:

- 로그(log): 요청 단위 상세 정보
- 메트릭(metric): 지연/오류율/처리량
- 트레이스(trace): 단계별 병목 추적

추천 대시보드:
1. 입력 품질 대시보드
2. 모델 성능/정확도 대시보드
3. 인프라 지연/오류 대시보드
4. 비용(FinOps) 대시보드

### 11) 사고 대응과 복구 전략

AI 시스템은 장애가 발생한다는 전제로 설계해야 합니다.

- degraded mode(간소화 응답) 지원
- 자동 롤백 조건
- 회로 차단기(circuit breaker) 패턴
- 재처리(replay) 가능한 이벤트 저장

장애 후에는 Postmortem을 통해 재발 방지 항목까지 문서화해야 합니다.

### 12) 비용 최적화(FinOps for AI)

정확도 향상 비용이 선형이 아닌 경우가 많습니다.  
따라서 "1% 성능 향상에 필요한 비용"을 추적해야 합니다.

실무 지표:

- cost per 1k requests
- GPU hour per training run
- cost per successful task
- latency-cost Pareto frontier

---

## 실무에서 자주 틀리는 포인트

1. 데이터 품질 검증을 학습 전에 한 번만 수행  
2. 스키마 변경을 알림 없이 배포하여 다운스트림 장애 발생  
3. feature 정의가 학습/서빙에서 다르게 구현됨  
4. point-in-time join 없이 단순 최신값 조인 사용  
5. 평균 지연만 보고 p99 tail latency를 무시  
6. 캐시 무효화 정책 없이 오래된 응답 반환  
7. 오토스케일 임계값 미설정으로 비용 급증 또는 장애  
8. 장애 복구(runbook/replay) 절차 없이 운영

---

## Python 샘플 코드 (데이터 품질 + PIT Join + 서빙 게이트)

데이터 품질 점검 템플릿:

```python
from dataclasses import dataclass
import pandas as pd


@dataclass
class QualityRule:
    null_ratio_max: float = 0.1
    min_value: float | None = None
    max_value: float | None = None
    allowed_values: set[str] | None = None


def check_quality(df: pd.DataFrame, rules: dict[str, QualityRule]) -> list[str]:
    issues = []
    for col, rule in rules.items():
        if col not in df.columns:
            issues.append(f"{col}: missing")
            continue
        null_ratio = float(df[col].isna().mean())
        if null_ratio > rule.null_ratio_max:
            issues.append(f"{col}: null_ratio={null_ratio:.3f} > {rule.null_ratio_max}")

        if rule.min_value is not None or rule.max_value is not None:
            s = pd.to_numeric(df[col], errors="coerce")
            if rule.min_value is not None and float(s.min(skipna=True)) < rule.min_value:
                issues.append(f"{col}: below min {rule.min_value}")
            if rule.max_value is not None and float(s.max(skipna=True)) > rule.max_value:
                issues.append(f"{col}: above max {rule.max_value}")

        if rule.allowed_values is not None:
            invalid = set(df[col].dropna().astype(str)) - rule.allowed_values
            if invalid:
                issues.append(f"{col}: invalid={sorted(invalid)}")
    return issues


sample = pd.DataFrame(
    {
        "age": [20, 35, None, 140],
        "tier": ["basic", "pro", "gold", "enterprise"],
    }
)

rules = {
    "age": QualityRule(null_ratio_max=0.2, min_value=0, max_value=120),
    "tier": QualityRule(null_ratio_max=0.0, allowed_values={"basic", "pro", "enterprise"}),
}
print(check_quality(sample, rules))
```

Point-in-time join(미래 누수 방지) 예시:

```python
import pandas as pd


events = pd.DataFrame(
    {
        "user_id": [1, 1, 2, 2],
        "event_time": pd.to_datetime(
            ["2026-01-01 09:00", "2026-01-02 10:00", "2026-01-01 11:00", "2026-01-03 09:00"]
        ),
        "feature_x": [0.2, 0.5, 0.1, 0.9],
    }
).sort_values(["user_id", "event_time"])

labels = pd.DataFrame(
    {
        "user_id": [1, 2],
        "label_time": pd.to_datetime(["2026-01-02 09:30", "2026-01-02 12:00"]),
        "label": [1, 0],
    }
).sort_values(["user_id", "label_time"])

# 핵심: label_time 이전 이벤트만 최근값으로 붙임
pit = pd.merge_asof(
    labels,
    events,
    by="user_id",
    left_on="label_time",
    right_on="event_time",
    direction="backward",
)
print(pit[["user_id", "label_time", "feature_x", "label"]])
```

서빙 릴리스 게이트(지연/비용/오류율) 예시:

```python
def serving_gate(metrics: dict) -> bool:
    """
    metrics 예시:
    {
      "p95_latency_ms": 280,
      "error_rate": 0.004,
      "cost_per_1k_req": 1.8,
      "timeout_rate": 0.002
    }
    """
    return (
        metrics["p95_latency_ms"] <= 350
        and metrics["error_rate"] <= 0.01
        and metrics["cost_per_1k_req"] <= 2.5
        and metrics["timeout_rate"] <= 0.005
    )


candidate = {
    "p95_latency_ms": 300,
    "error_rate": 0.006,
    "cost_per_1k_req": 2.1,
    "timeout_rate": 0.003,
}
print("promote:", serving_gate(candidate))
```

---

## 미니 과제

1. 본인 데이터셋에 품질 규칙 10개를 정의하고 자동 검사 스크립트 작성  
2. 최근 3개월 이벤트 로그를 대상으로 point-in-time 학습셋 생성  
3. 스키마 변경(컬럼 추가/타입 변경) 시나리오 2개에 대한 대응 runbook 작성  
4. p95/p99 지연과 비용을 동시에 측정하는 서빙 대시보드 설계  
5. 캐시 hit ratio를 포함한 성능 실험(캐시 on/off) 비교  
6. 장애 상황(데이터 지연/추론 타임아웃) 재현 후 롤백 절차 검증

---

## 핵심 용어

- Data Contract, Schema Evolution, Backfill
- Completeness, Freshness, Validity, Consistency
- Feature Store, Offline/Online Parity, Training-Serving Skew
- Point-in-time Join, Data Leakage
- Online Serving, Batch Inference, Async Queue
- Tail Latency(p95/p99), Throughput, Dynamic Batching
- Autoscaling, Circuit Breaker, Degraded Mode
- FinOps, Cost per Request, Pareto Frontier

---

## 추천 자료

- 도서: `Designing Data-Intensive Applications`
- 도서: `Fundamentals of Data Engineering`
- 도서: `Designing Machine Learning Systems`
- 문서: [Google SRE Workbook](https://sre.google/workbook/)
- 문서: [Feast (Feature Store)](https://docs.feast.dev/)

---

## 이해도 점검 질문

1. batch와 stream 파이프라인을 함께 쓰는 하이브리드 구조가 필요한 이유는 무엇인가?  
2. schema evolution을 잘못 다루면 어떤 종류의 장애가 발생하는가?  
3. point-in-time join이 없는 학습셋이 왜 위험한가?  
4. offline-online parity를 보장하지 않으면 어떤 운영 문제가 생기는가?  
5. 평균 지연 대신 p95/p99를 관리해야 하는 이유는 무엇인가?  
6. 캐시를 도입했을 때 정확도/신선도 측면에서 어떤 부작용이 생길 수 있는가?  
7. 오토스케일 정책을 잘못 설정하면 비용과 안정성에 어떤 영향이 있는가?  
8. AI FinOps 지표를 운영하지 않으면 장기적으로 어떤 문제가 생기는가?

---

최상위 허브: [0단계_학습자료.md](0단계_학습자료.md)
